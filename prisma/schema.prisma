generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model tasks {
  id             Int      @id @default(autoincrement())
  title          String?  @db.VarChar(50)
  descriptiontsk String?  @db.VarChar(100)
  completed      Boolean? @default(false)
}
